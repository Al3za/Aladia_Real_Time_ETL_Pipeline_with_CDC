# Registrare Debezium (CDC su PostgreSQL)

in questo file .json comunichiamo a Debezium il mio db postgress e la tabella employee.
in poche parole gli stiamo dicendo dove guardare per intercettare i cambiamenti dei dati entro questo db e tabella e poter fare CDC
infatti i dati si database.hostname: e database.dbname: sono parte della stringa di .env DATABASE_URL(la nostra connection al db). mentre employee e'la nostra tabella
👉

{
"name": "postgres-employee-connector",
"config": {
"connector.class": "io.debezium.connector.postgresql.PostgresConnector",
"database.hostname": "ep-royal-bird-abwk6kxv-pooler.eu-west-2.aws.neon.tech", // lo troviamo in env DATABASE_URL
"database.port": "5432",
"database.user": "neondb_owner",
"database.password": "npg_5uamkynztKW3",
"database.dbname": "Ale-nestjsDB", // lo troviamo in .env DATABASE_URL
"database.sslmode": "require",

"topic.prefix": "cdc",
"table.include.list": "public.Employee",

"plugin.name": "pgoutput",
"slot.name": "debezium_employee",
"publication.autocreate.mode": "filtered"
}
}

{
  "name": "postgres-employee-connector",
  "config": {
    "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
    "database.hostname": "ep-royal-bird-abwk6kxv-pooler.eu-west-2.aws.neon.tech",
    "database.port": "5432",
    "database.user": "debezium_user",
    "database.password": "strong_password",
    "database.dbname": "Ale-nestjsDB",
    "database.sslmode": "require",
    "topic.prefix": "cdc",
    "table.include.list": "public.Employee",
    "plugin.name": "pgoutput",
    "slot.name": "debezium_employee",
    "publication.autocreate.mode": "filtered"
  }
}